import Form from './Form';

const ResetPassword = () => {
  return (
    <div data-testid="reset-password-container" className="reset-password-background">
      <div className="pt-40">
        <Form />
      </div>
    </div>
  );
};

export default ResetPassword;
